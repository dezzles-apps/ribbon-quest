info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: https://ribbon-quest.dezzles-apps.com/api/auth/v1/register
  body:
    type: json
    data: |-
      {
        "username": "dezzles",
        "password": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
