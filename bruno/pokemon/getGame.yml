info:
  name: getGame
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8083/api/ribbons/v1/games/Y
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
