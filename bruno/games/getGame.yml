info:
  name: getGame
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5173/api/games/v1/OMEGA_RUBY
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
